# v1.0.0 — Workflow Hardening and External Readiness

**Date:** July 2026  
**Theme:** Durable workers, DAG workflows, custom suites, long-transcript honesty, and safety-aware reports.

## Highlights

| Area | Delivered |
|------|-----------|
| **Dedicated worker** | ECS `rre-dev-worker` (`RRE_PROCESS=worker`); API queues `CREATED` jobs; cancel / timeout / retries |
| **Workflow DAG** | YAML `steps` with `depends_on` + `parallel`; topo waves; `full_mvp` migrated |
| **Custom workflows** | `POST /api/workflows/custom/run` + Streamlit builder |
| **Long transcripts** | Balanced quote sampling; explicit strategy; `GET …/length-assessment` |
| **Safety mode** | Risk scanner, `safety_mode` on runs, UI banner, synthesis framing, exploratory skip |
| **Migrations** | Alembic `010`–`011` |

## Migrations

- `010_workflow_job_control`
- `011_safety_mode`

## Docs

Application guides under `docs/` updated for v1.0.0. Plans: [../architecture/workflow_dag_plan.md](../architecture/workflow_dag_plan.md), [../product/safety_aware_report_mode.md](../product/safety_aware_report_mode.md).

## Next

**Post-v1.0** — deferred backlog ([../planning/future_considerations.md](../planning/future_considerations.md), [../planning/implementing.md](../planning/implementing.md)).
