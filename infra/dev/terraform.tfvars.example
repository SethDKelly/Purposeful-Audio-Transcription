# Optional overrides for local terraform plan/apply.
# CI passes -var="image_tag=$GITHUB_SHA" on deploy.

# image_tag = "local-dev"

# VPC endpoints stay on by default. Strict no-egress (no public IPs) is opt-in:
# enable_no_egress_networking = true
# enable_vpc_endpoints = true
