# Safety red-team fixtures (v1.2 Workstream B)
#
# Each subdirectory contains:
#   transcript.txt              — labeled dialogue
#   metadata.yaml               — fixture_id, categories, expected UI mode
#   expected_assertions.yaml    — scanner + report behavior expectations

layout_version: 1
