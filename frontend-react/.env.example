VITE_API_BASE_URL=
# Break-glass only — product auth is email OTP + session cookie (credentials: include).
# Leave empty for normal React use; API key is for tooling / emergency access.
VITE_API_KEY=
# Local vite proxy target when VITE_API_BASE_URL is empty:
# VITE_API_PROXY=http://127.0.0.1:8000
